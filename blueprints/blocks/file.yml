name: block.name
icon: attachment

fields:
  doctitle:
    type: text
    label: block.title.label

  file:
    label: block.file.label
    type: files
    query: page.files.template('download-file')
    uploads: download-file
    multiple: false